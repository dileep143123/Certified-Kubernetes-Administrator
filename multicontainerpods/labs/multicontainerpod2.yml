# in this multiple containers will communicate with each other over localhost

apiVersion: v1
kind: Pod
metadata:
  name: multi-container-pod2
spec:
  containers:
    - name: webapp
      image: nginx:latest
    - name: client
      image: busybox:1.36
      command: ["sh", "-c", "sleep 4800"]
