# create the taint on the node first:
# kubectl taint nodes <node-name> app=red:NoExecute

apiVersion: v1
kind: Pod
metadata:
  name: noexecute-toleration-pod
  labels:
    app: nginx
    lab: taints
spec:
  tolerations:
    - key: "app"
      operator: "Equal"
      value: "red"
      effect: "NoExecute"
      tolerationSeconds: 30
  containers:
    - name: nginx
      image: nginx:1.25-alpine
      ports:
        - containerPort: 80